{% extends 'base.html.twig' %}

{% block title %}Videos{% endblock %}

{% block body %}
    <h1>Videos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video.id }}</td>
            </tr>
            <tr>
                <th>Vi_name</th>
                <td>{{ video.viName }}</td>
            </tr>
            <tr>
                <th>Vi_desc</th>
                <td>{{ video.viDesc }}</td>
            </tr>
            <tr>
                <th>Vi_path</th>
                <td>{{ video.viPath }}</td>
            </tr>
            <tr>
                <th>Vi_trick_id</th>
                <td>{{ video.viTrickId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('videos_index') }}">back to list</a>

    <a href="{{ path('videos_edit', {'id': video.id}) }}">edit</a>

    {{ include('videos/_delete_form.html.twig') }}
{% endblock %}
