    {% extends 'base.html.twig' %}

    {% block title %}Trick details{% endblock %}

    {% block body %}
        <h1>Trick details</h1>
        <hr>

        <aside style="width:35%; float:left">
            <img src="../img/grab-mute-large.jpg" alt="Thumbnail"/>
        </aside>

      <main style="width:60%; float:left">
      <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ trick.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ trick.title }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ trick.description }}</td>
                </tr>

                <tr>
                    <th>Author</th>
                    <td>{{ trick.creaAuthor }}</td>
                </tr>

                <tr>
                    <th>Date</th>
                    <td>{{ trick.creaDate | date("d/m/y \\à H:m") }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('trick_index') }}">back to list</a>

        <a href="{{ path('trick_edit', {'id': trick.id}) }}">edit</a>

        {{ include('trick/_delete_form.html.twig') }}

        </main>

    {% endblock %}
