{% extends 'base.html.twig' %}

{% block title %}
    {{page_title}}
{% endblock %}

{% block body %}
    <h1>{{page_title}}</h1>

    {{ include('trick/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-sm btn-info" href="{{ path('trick_index') }}">back to list</a>

    {{ include('trick/_delete_form.html.twig') }}
{% endblock %}
