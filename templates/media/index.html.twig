{% extends 'base.html.twig' %}

{% block title %}Media index{% endblock %}

{% block body %}
    <h1>Media index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>MediaName</th>
                <th>CreatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for media in medias %}
            <tr>
                <td>{{ media.id }}</td>
                <td>{{ media.name }}</td>
                <td>{{ media.mediaName }}</td>
                <td>{{ media.createdAt ? media.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    {# <a href="{{ path('media_show', {'id': media.id}) }}">show</a> #}
                    {# <a href="{{ path('media_edit', {'id': media.id}) }}">edit</a> #}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('media_new') }}">Create new</a> #}
{% endblock %}
