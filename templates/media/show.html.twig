{% extends 'base.html.twig' %}

{% block title %}Media{% endblock %}

{% block body %}
    <h1>Media</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medias.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ medias.name }}</td>
            </tr>
               <tr>
                <th>MediaName</th>
                <td>{{ medias.mediaName }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ medias.createdAt ? medias.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('media_index') }}">back to list</a>

    <a href="{{ path('media_edit', {'id': medias.id}) }}">edit</a>

    {{ include('media/_delete_form.html.twig') }}
{% endblock %}
