{% extends 'base.html.twig' %}

{% block title %}Groups{% endblock %}

{% block body %}
    <h1>Groups</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group.id }}</td>
            </tr>
            <tr>
                <th>Gr_name</th>
                <td>{{ group.grName }}</td>
            </tr>
            <tr>
                <th>Gr_desc</th>
                <td>{{ group.grDesc }}</td>
            </tr>
            <tr>
                <th>Gr_active</th>
                <td>{{ group.grActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('groups_index') }}">back to list</a>

    <a href="{{ path('groups_edit', {'id': group.id}) }}">edit</a>

    {{ include('groups/_delete_form.html.twig') }}
{% endblock %}
