{% extends 'base.html.twig' %}

{% block title %}Group{% endblock %}

{% block body %}
    <h1>Group</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group.id }}</td>
            </tr>
            <tr>
                <th>Gr_name</th>
                <td>{{ group.grName }}</td>
            </tr>
            <tr>
                <th>Gr_desc</th>
                <td>{{ group.grDesc }}</td>
            </tr>
            <tr>
                <th>Gr_crea_date</th>
                <td>{{ group.grCreaDate ? group.grCreaDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('group_index') }}">back to list</a>

    <a href="{{ path('group_edit', {'id': group.id}) }}">edit</a>

    {{ include('group/_delete_form.html.twig') }}
{% endblock %}
